-module(adb_db).

-export([start/0, stop/0, dump/0, fail/0, recover/0]).

start() ->
	ok.

stop() ->
    ok.

%%--------------------------------------------------------------------
%% Function: dump() -> {ok, SiteId, Variables}
%%--------------------------------------------------------------------
dump() ->
    {ok, 1, []}.

%%--------------------------------------------------------------------
%% Function: fail() -> {ok, Time}
%%--------------------------------------------------------------------    
fail() ->
    {ok, time()}.

%%--------------------------------------------------------------------
%% Function: recover() -> {ok, Time}
%%--------------------------------------------------------------------    
recover() ->
    {ok, time()}.
%%====================================================================
%% Internal functions
%%====================================================================


